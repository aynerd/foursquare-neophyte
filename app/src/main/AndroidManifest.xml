<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inveniotechnologies.neophyte">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="keyboardHidden|orientation"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.Home"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.AddRecord"
            android:label="Register" />
        <activity
            android:name=".ui.AllRecords"
            android:label="People" />
        <activity
            android:name=".ui.RecordDetails"
            android:label="Details" />
        <activity
            android:name=".ui.UpdateRecord"
            android:label="Edit Details"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBQM9OkY4x_aKNfzJdPFqKNx2lGUrjoKRE" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>